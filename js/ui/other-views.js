import { getGame, getState, resetV2 } from '../core/store.js';
function list(title,rows){return `<h2>${title}</h2><div class="simple-list">${rows.length?rows.map(([a,b])=>`<div class="list-row"><span>${a}</span><b>${b}</b></div>`).join(''):'<div class="card empty">目前沒有資料。</div>'}</div>`}
export function renderShop(root){const g=getGame();root.innerHTML=list('🛒 商店',(g.shopItems||[]).filter(x=>x.active!==false).slice(0,20).map(x=>[x.name,`💰 ${x.price??'-'}`]))}
export function renderLottery(root){const g=getGame();root.innerHTML=`<h2>🎰 抽獎</h2><div class="card"><div class="kid-box">🪙 ${Number(g.hero.lotteryCoins)||0}</div><div class="small" style="margin-top:8px">2.0 目前只顯示 1.0 抽獎池資料；抽獎交易會在 inventory transaction 模組完成後接回。</div></div>${list('獎池',(g.lotteryPool||[]).filter(x=>x.active!==false).map(x=>[x.label||x.name,`權重 ${x.weight??'-'}`]))}`}
export function renderBag(root){const g=getGame();root.innerHTML=list('🎒 背包',(g.inventory||[]).slice(0,50).map(x=>[x.name||x.itemId||'物品',x.status||'unused']))}
export function renderSocial(root){const g=getGame(),s=g.social||{};root.innerHTML=`<h2>🤝 好友與線上冒險</h2><div class="grid3"><div class="card"><h3>👥 好友</h3><b>${(s.friends||[]).length}</b></div><div class="card"><h3>⚔️ PVP</h3><div class="small">Provider 未接入</div></div><div class="card"><h3>✉️ 信箱</h3><b>${(s.inbox||[]).length}</b></div></div>`}
export function renderGM(root,rerender){const s=getState(),g=getGame();root.innerHTML=`<h2>🛠 家長後台</h2><div class="card"><h3>2.0 架構狀態</h3><div class="simple-list"><div class="list-row"><span>資料來源</span><b>${s.source}</b></div><div class="list-row"><span>1.0 原始存檔</span><b>只讀，不覆寫</b></div><div class="list-row"><span>週世界資料</span><b>單一 World Database</b></div><div class="list-row"><span>Scene 座標</span><b>Visual Config 單一來源</b></div><div class="list-row"><span>目前 index</span><b>只負責 App Shell</b></div></div></div><div class="card"><h3>測試資料</h3><button id="reset2" class="action-button red">清除 2.0 測試存檔並重新讀取 1.0</button><div class="small" style="margin-top:7px">不刪除 heroRPG_FAMILY_V8。</div></div><div class="dev-note">目前已拆：Store / Legacy Reader / World / Visual / Status / Exam Energy / Calendar Timeline / Tasks / Dungeon。Battle、Inventory Transaction、Firebase Provider 尚未搬入。</div>`;root.querySelector('#reset2').addEventListener('click',()=>{resetV2();rerender()})}
