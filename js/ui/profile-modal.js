import { getFamily, switchProfile } from '../core/store.js';
export function showProfileModal(onChange){document.getElementById('profileModal')?.remove();const f=getFamily(),m=document.createElement('div');m.id='profileModal';m.className='modal-overlay';m.innerHTML=`<div class="modal-box"><h2>今天是誰要冒險？</h2>${f.profiles.map(p=>`<div class="list-row"><div><b>${p.data?.hero?.gender==='female'?'👧':'👦'} ${p.data?.hero?.name||'尚未建立'}</b><div class="small">Lv.${p.data?.hero?.level||1}｜${p.data?.hero?.jobAwakened?p.data.hero.heroClass:'見習勇者'}</div></div><button class="action-button ${p.id===f.activeProfileId?'primary':'blue'}" data-profile="${p.id}">${p.id===f.activeProfileId?'使用中':'切換'}</button></div>`).join('')}<button id="closeProfile" class="action-button" style="width:100%;margin-top:10px">關閉</button></div>`;document.body.appendChild(m);m.querySelectorAll('[data-profile]').forEach(b=>b.addEventListener('click',()=>{switchProfile(b.dataset.profile);m.remove();onChange()}));m.querySelector('#closeProfile').onclick=()=>m.remove();m.addEventListener('click',e=>{if(e.target===m)m.remove()})}
